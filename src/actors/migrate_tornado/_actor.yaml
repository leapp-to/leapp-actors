inputs:
- name: process_list_filter
  type:
    name: BaseTypeString
description: |
    Migrate tornado applications found on the source system

    Inputs:
        process_list_filter        - filter results by word contained in /proc/<pid>/cmdline

group:
- process_list
- tornado_scanner
- tornado_generator
